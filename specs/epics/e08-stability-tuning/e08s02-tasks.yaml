story_id: e08s02
title: "Threshold tuning via replay until CV targets met"
status: todo
bcps: 8
tasks:
  - id: e08s02t01
    description: >
      For each dimension above target: diagnose via the two CVs (routing flap ->
      sharpen Router identity rule; sizing flap -> tune decision-table thresholds
      via offline replay). Each accepted change bumps calibration_id (ADR-0010).
    verify: "venv/bin/python run_stability.py --report tests/results/stability/ | grep -q 'TARGETS: PASS'"
    status: todo
  - id: e08s02t02
    description: >
      Final verification run (fresh recorded iterations, not replay) confirming
      targets hold live; report committed with the release calibration_id.
    verify: "venv/bin/python run_stability.py --report tests/results/stability/ | grep -q 'TARGETS: PASS'"
    status: todo
